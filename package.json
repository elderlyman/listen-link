{
  "name": "listen-link",
  "version": "0.1.0",
  "private": true,
  "description": "Recipient-first music link resolver prototype using fake catalog data.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
